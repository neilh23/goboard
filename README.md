Go (Baduk/Weiqi) board in canvas.

Mostly for playing around with some ideas, but might turn into something

Building:

sudp npm install -g gulp eslint babel-eslint uglify
npm install --save-dev gulp-babel gulp-uglify
